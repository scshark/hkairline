name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: ssh deploy
      uses: appleboy/ssh-action@master
      env:
            host: 120.78.75.92
            username: root
            password: OUshaoHUI0520...
            script: whoami
                    ll
